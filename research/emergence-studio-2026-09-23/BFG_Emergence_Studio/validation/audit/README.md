# Empirical Audit Archive

This directory stores immutable confirmatory provenance that is not part of the
active validation portfolio.

The active portfolio is defined only by `EMPIRICAL_BENCHMARK_REGISTRY.json`.
Audit records are retained separately so current project status stays focused on
active hypotheses while provenance remains recoverable for formal review.

Audit material must not be used to alter an already frozen active held-out plan.
