# Reading and provenance note

Date: 23 September 2026. Scope: archive inventory, comparison, documentation and supplied-result reading; selected source text inspected without execution. No independent full proof audit or runtime certification is claimed.

## Meaning of the new status

The new ledger explicitly adopts the intrinsic active-Gram law, square-root factor gauge, phase-independent formation density and fixed unitary successor recursion as architectural rules of a reduced finite model. Its CLOSED status is scoped to that declared category. This is consistent with using an explicitly selected model to remove an operational choice: it does not establish that older axioms uniquely implied those choices. The source itself distinguishes those two claims.

The supplied material also distinguishes its restricted bounded infinite-dimensional finite-peripheral-rank entry class from arbitrary unbounded/infinite-rank transport. Empirical natural realization remains separate. Earlier negative empirical records remain in the archive and are not erased by the new runtime status.

## What the added mathematics says

- Finite power boundedness requires spectrum in the closed unit disk and semisimple unit-circle eigenvalues, not spectral radius alone.
- Supplied rational invariant splits can be checked through an isometric peripheral metric and a strictly contractive stable metric. The certificate verifies supplied data; it does not automatically discover every split.
- The spectral/oblique projector and the graph-metric projector remain distinct.
- Positive supplied Gram factors reconstruct a positive load. The adopted finite profile makes its own explicit factor assignment.
- Retained-channel Gram energy decomposes into tangent and external energy; the energy matrix alone does not reconstruct external directions or arbitrary future couplings.

## Software-reading boundary

The archive adds `bfg_studio/finite_closure.py` and its separate reduced-state API. The earlier `core.py`, `carrier.py` and `simulate.py` are byte-identical to the first supplied snapshot. Some CLI paths still call that existing simulation API. Therefore the documentation's adoption of a finite profile should not be read as evidence that every legacy entry point was replaced. This import does not execute or independently certify dispatch behavior.

One wording qualification is retained for later mathematical review: Section 9 of the stability theorem needs a restriction on perturbations if it intends preservation of exact unit-circle persistence or power boundedness. For R=diag(1,1/2), replacing the first eigenvalue by 1+epsilon is arbitrarily small and preserves simplicity, but loses power boundedness. Spectral-cluster separation is a weaker conclusion. This reading observation does not alter the supplied document or its finite power-boundedness criterion.

## Provenance and preservation

Archive SHA-256: `bb2fe029b81f8a4f48d6fe3c1e195a93aa2ddb86058b2b8385f2890b07d7f190`.

There are 314 file payloads totalling 6,639,448 bytes. Paths were checked before extraction; original file SHA-256 and Git blob identities are listed in IMPORT_MANIFEST.json. No tests, simulations, held-out evaluations, source edits or result regeneration were performed. A bounded scan found no private-key header, GitHub-token or AWS-access-key patterns; this is not a security certification.

The documented results are preserved as results of this BFG version. No independent rerun was performed during import. Existing paths recorded inside result manifests, such as `/mnt/data/...`, are historical execution provenance, not newly asserted local paths. No source fingerprint or validation seal was regenerated.
