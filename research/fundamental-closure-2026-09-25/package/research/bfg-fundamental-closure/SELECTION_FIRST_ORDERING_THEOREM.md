# Selection-First Ordering Theorem

## Source order

The pure BFG monograph gives the structural spine

\[
A_0\to\delta\to(P_1,P_2)\to\mathrm{Rel}\to N\to K\to C\to A\to S_A\to T_A.
\]

In particular,

\[
\boxed{
A\to S_A\to T_A
}
\]

places closure-positive selection after attractor/persistence has been established and
before identity-preserving transport to the next closure.

## Consequence

A master update that first projects every branch into the old persistent space and only
afterwards applies Selection is structurally biased toward persistence preservation.

The source-faithful finite ordering is instead:

\[
\boxed{
\Xi_n
\to
Z(Y_n)
\to
Q_n^{\rm sel}
\to
T_n^{\rm sel}
\to
\Xi_n^{\rm surviving}
\to
\mathcal A_n
\to
J_n
\to
\Xi_{n+1}.
}
\]

Here

\[
Q_n^{\rm sel}
=
\mathbf1_{(0,\infty)}(Z(Y_n))
\]

under the Neutral-Contrast Selection Principle, and

\[
T_n^{\rm sel}
=
\operatorname{polar}(Q_n^{\rm sel}P_n)
\]

is the R4 transport from old persistence into the currently closure-positive source
sector.

This permits exact source persistent-rank loss whenever the closure-positive selected
support has insufficient overlap rank with the old persistent sector.

No floating spectral window is required.

## Epistemic boundary

The **ordering** \(A\to S_A\to T_A\) is source BFG.

The **specific identification**
\[
S_A \equiv \mathbf1_{(0,\infty)}(Z)
\]
is the new Neutral-Contrast Selection Principle of this living package.
